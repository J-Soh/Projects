# Basic calculator app from scratch 
- This app does not include external libraries like React etc. 
- It includes HTML (page structure), CSS (page style) & JavaScript (page functions). 

## How to run this basic app:

1. Do `git clone https://github.com/J-Soh/Calculator-App.git`
2. In root folder > run `python -m http.server 8000`
